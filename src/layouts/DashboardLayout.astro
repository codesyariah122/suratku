---
import { supabase } from '../lib/supabase'
import '../styles/global.css'

interface Props {
  title?: string
}

const { title = 'Dashboard - SuratKu' } = Astro.props
---

<!doctype html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />

    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <meta name="description" content="SuratKu - Buat surat resmi dengan mudah." />

    <link rel="icon" type="image/png" href="/images/logo-suratku.png" />

    <title>{title}</title>

    <link rel="preconnect" href="https://fonts.googleapis.com" />

    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Alegreya:wght@400;600;700&family=Bitter:wght@400;600;700&family=Crimson+Pro:wght@400;600;700&family=DM+Sans:wght@400;600;700&family=DM+Serif+Display&family=EB+Garamond:wght@400;600;700&family=Fira+Sans:wght@400;600;700&family=IBM+Plex+Mono:wght@400;600&family=IBM+Plex+Sans:wght@400;600;700&family=Inter:wght@400;600;700&family=Libre+Baskerville:wght@400;700&family=Lora:wght@400;600;700&family=Manrope:wght@400;600;700&family=Merriweather:wght@400;700&family=Montserrat:wght@400;600;700&family=Nunito+Sans:wght@400;600;700&family=Open+Sans:wght@400;600;700&family=Oswald:wght@400;600;700&family=Playfair+Display:wght@400;600;700&family=Poppins:wght@400;600;700&family=Raleway:wght@400;600;700&family=Roboto:wght@400;500;700&family=Roboto+Slab:wght@400;600;700&family=Source+Sans+3:wght@400;600;700&family=Source+Serif+4:wght@400;600;700&family=Space+Grotesk:wght@400;600;700&family=Ubuntu:wght@400;500;700&display=swap"
      rel="stylesheet"
    />
  </head>

  <body>
    <div class="dashboard-layout">
      <!-- ========================================
           SIDEBAR
      ========================================= -->

      <aside class="dashboard-sidebar" id="dashboard-sidebar">
        <!-- BRAND -->
        <div class="sidebar-header">
          <a href="/dashboard" class="sidebar-brand"
            ><img src="/images/logo-suratku.png" alt="" /><span>SuratKu</span></a
          >
        </div>

        <!-- NAVIGATION -->
        <nav class="sidebar-nav">
          <div class="sidebar-section">
            <span class="sidebar-section-title"> MENU </span>

            <a href="/dashboard" class="sidebar-link" data-nav-link="/dashboard">
              <span class="sidebar-link-icon">
                <svg
                  width="18"
                  height="18"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                >
                  <rect x="3" y="3" width="7" height="7"></rect>
                  <rect x="14" y="3" width="7" height="7"></rect>
                  <rect x="14" y="14" width="7" height="7"></rect>
                  <rect x="3" y="14" width="7" height="7"></rect>
                </svg>
              </span>

              <span> Dashboard </span>
            </a>

            <a href="/dashboard/buat" class="sidebar-link" data-nav-link="/dashboard/buat">
              <span class="sidebar-link-icon">
                <svg
                  width="18"
                  height="18"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                >
                  <path d="M12 20h9"></path>
                  <path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4Z"></path>
                </svg>
              </span>

              <span> Buat Surat </span>
            </a>

            <a
              href="/dashboard/templates"
              class="sidebar-link"
              data-nav-link="/dashboard/templates"
            >
              <span class="sidebar-link-icon"
                ><svg
                  width="18"
                  height="18"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  ><path d="M4 4h16v16H4z"></path><path d="M8 8h8M8 12h8M8 16h5"></path></svg
                ></span
              >
              <span>Template Surat</span>
            </a>

            <a href="/dashboard/riwayat" class="sidebar-link" data-nav-link="/dashboard/riwayat">
              <span class="sidebar-link-icon">
                <svg
                  width="18"
                  height="18"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                >
                  <path d="M3 12a9 9 0 1 0 3-6.7"></path>
                  <path d="M3 4v6h6"></path>
                  <path d="M12 7v5l3 2"></path>
                </svg>
              </span>

              <span> Riwayat </span>
            </a>

            <a
              href="/dashboard/pengaturan"
              class="sidebar-link"
              data-nav-link="/dashboard/pengaturan"
            >
              <span class="sidebar-link-icon">
                <svg
                  width="18"
                  height="18"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                >
                  <circle cx="12" cy="12" r="3"></circle>
                  <path
                    d="M19.4 15a1.7 1.7 0 0 0 .34 1.88l.06.06-1.7 1.7-.06-.06a1.7 1.7 0 0 0-1.88-.34 1.7 1.7 0 0 0-1.03 1.56V20h-2.4v-.08a1.7 1.7 0 0 0-1.03-1.56 1.7 1.7 0 0 0-1.88.34l-.06.06-1.7-1.7.06-.06A1.7 1.7 0 0 0 8.46 15a1.7 1.7 0 0 0-1.56-1.03H6V11.6h.08A1.7 1.7 0 0 0 7.64 10a1.7 1.7 0 0 0-.34-1.88l-.06-.06 1.7-1.7.06.06A1.7 1.7 0 0 0 10.88 6.1 1.7 1.7 0 0 0 11.9 4.54V4h2.4v.08a1.7 1.7 0 0 0 1.03 1.56 1.7 1.7 0 0 0 1.88-.34l.06-.06 1.7 1.7-.06.06A1.7 1.7 0 0 0 18.58 9a1.7 1.7 0 0 0 1.56 1.03H20v2.4h-.08A1.7 1.7 0 0 0 18.46 15Z"
                  ></path>
                </svg>
              </span>

              <span> Pengaturan </span>
            </a>
          </div>
        </nav>

        <!-- SIDEBAR FOOTER -->
        <div class="sidebar-footer">
          <button type="button" id="logout-button" class="sidebar-logout">
            <span class="sidebar-link-icon">
              <svg
                width="18"
                height="18"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
              >
                <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path>
                <polyline points="16 17 21 12 16 7"></polyline>
                <line x1="21" y1="12" x2="9" y2="12"></line>
              </svg>
            </span>

            <span> Keluar </span>
          </button>
        </div>
      </aside>

      <!-- ========================================
           MOBILE OVERLAY
      ========================================= -->

      <div class="sidebar-overlay" id="sidebar-overlay"></div>

      <!-- ========================================
           MAIN AREA
      ========================================= -->

      <div class="dashboard-main">
        <!-- MOBILE HEADER -->
        <header class="mobile-dashboard-header">
          <button type="button" id="sidebar-toggle" class="sidebar-toggle" aria-label="Buka menu">
            <svg
              width="22"
              height="22"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <line x1="4" y1="6" x2="20" y2="6"></line>
              <line x1="4" y1="12" x2="20" y2="12"></line>
              <line x1="4" y1="18" x2="20" y2="18"></line>
            </svg>
          </button>

          <a href="/dashboard" class="mobile-brand"
            ><img src="/images/logo-suratku.png" alt="" /><span>SuratKu</span></a
          >
        </header>

        <!-- PAGE CONTENT -->
        <main class="dashboard-content">
          <slot />
        </main>
      </div>
    </div>

    <div id="app-dialog" class="app-dialog" hidden>
      <div class="app-dialog-backdrop" data-dialog-cancel></div>
      <section
        class="app-dialog-card"
        role="dialog"
        aria-modal="true"
        aria-labelledby="app-dialog-title"
      >
        <button class="app-dialog-close" type="button" data-dialog-cancel aria-label="Tutup"
          >×</button
        >
        <div id="app-dialog-icon" class="app-dialog-icon">!</div>
        <span id="app-dialog-eyebrow" class="app-dialog-eyebrow">KONFIRMASI</span>
        <h2 id="app-dialog-title">Konfirmasi tindakan</h2>
        <p id="app-dialog-message"></p>
        <div class="app-dialog-actions">
          <button id="app-dialog-cancel" class="secondary-button" type="button" data-dialog-cancel
            >Batal</button
          >
          <button id="app-dialog-confirm" class="primary-button" type="button">Lanjutkan</button>
        </div>
      </section>
    </div>

    <!-- ========================================
         SCRIPT
    ========================================= -->

    <script>
      import { supabase } from '../lib/supabase'

      type DialogOptions = {
        title?: string
        message: string
        confirmText?: string
        cancelText?: string
        tone?: 'danger' | 'info' | 'success'
        eyebrow?: string
        alert?: boolean
      }
      const dialog = document.querySelector('#app-dialog') as HTMLElement
      const dialogCard = dialog.querySelector('.app-dialog-card') as HTMLElement
      const dialogTitle = document.querySelector('#app-dialog-title') as HTMLElement
      const dialogMessage = document.querySelector('#app-dialog-message') as HTMLElement
      const dialogEyebrow = document.querySelector('#app-dialog-eyebrow') as HTMLElement
      const dialogIcon = document.querySelector('#app-dialog-icon') as HTMLElement
      const dialogConfirm = document.querySelector('#app-dialog-confirm') as HTMLButtonElement
      const dialogCancel = document.querySelector('#app-dialog-cancel') as HTMLButtonElement
      let resolveDialog: ((result: boolean) => void) | null = null

      function closeDialog(result: boolean) {
        if (dialog.hidden) return
        dialog.hidden = true
        document.body.classList.remove('modal-open')
        resolveDialog?.(result)
        resolveDialog = null
      }

      function showDialog(options: DialogOptions) {
        if (resolveDialog) closeDialog(false)
        dialogTitle.textContent = options.title || 'Konfirmasi tindakan'
        dialogMessage.textContent = options.message
        dialogEyebrow.textContent = options.eyebrow || (options.alert ? 'INFORMASI' : 'KONFIRMASI')
        dialogConfirm.textContent =
          options.confirmText || (options.alert ? 'Mengerti' : 'Lanjutkan')
        dialogCancel.textContent = options.cancelText || 'Batal'
        dialogCancel.hidden = Boolean(options.alert)
        const tone = options.tone || 'info'
        dialogCard.dataset.tone = tone
        dialogIcon.textContent = tone === 'danger' ? '!' : tone === 'success' ? '✓' : 'i'
        dialog.hidden = false
        document.body.classList.add('modal-open')
        setTimeout(() => dialogConfirm.focus(), 0)
        return new Promise<boolean>((resolve) => {
          resolveDialog = resolve
        })
      }

      ;(
        window as Window & {
          suratkuDialog?: {
            confirm: (options: DialogOptions) => Promise<boolean>
            alert: (options: DialogOptions) => Promise<boolean>
          }
        }
      ).suratkuDialog = {
        confirm: showDialog,
        alert: (options) => showDialog({ ...options, alert: true }),
      }
      dialogConfirm.addEventListener('click', () => closeDialog(true))
      dialog
        .querySelectorAll('[data-dialog-cancel]')
        .forEach((element) => element.addEventListener('click', () => closeDialog(false)))
      document.addEventListener('keydown', (event) => {
        if (event.key === 'Escape' && !dialog.hidden) closeDialog(false)
      })

      // ========================================
      // CHECK AUTH
      // ========================================

      async function checkUser() {
        const {
          data: { user },
        } = await supabase.auth.getUser()

        if (!user) {
          window.location.href = '/login'
          return
        }
      }

      checkUser()

      // ========================================
      // LOGOUT
      // ========================================

      const logoutButton = document.getElementById('logout-button')

      logoutButton?.addEventListener('click', async () => {
        logoutButton.setAttribute('disabled', 'true')

        const { error } = await supabase.auth.signOut()

        if (error) {
          console.error('Logout error:', error)

          logoutButton.removeAttribute('disabled')

          return
        }

        window.location.href = '/login'
      })

      // ========================================
      // ACTIVE MENU
      // ========================================

      const currentPath = window.location.pathname

      const navLinks = document.querySelectorAll('[data-nav-link]')

      navLinks.forEach((link) => {
        const href = link.getAttribute('data-nav-link')

        if (!href) {
          return
        }

        if (href === '/dashboard' && currentPath === '/dashboard') {
          link.classList.add('active')
          return
        }

        if (href !== '/dashboard' && currentPath.startsWith(href)) {
          link.classList.add('active')
        }
      })

      // ========================================
      // MOBILE SIDEBAR
      // ========================================

      const sidebar = document.getElementById('dashboard-sidebar')

      const overlay = document.getElementById('sidebar-overlay')

      const toggle = document.getElementById('sidebar-toggle')

      function openSidebar() {
        sidebar?.classList.add('open')
        overlay?.classList.add('open')
        document.body.classList.add('sidebar-open')
      }

      function closeSidebar() {
        sidebar?.classList.remove('open')
        overlay?.classList.remove('open')
        document.body.classList.remove('sidebar-open')
      }

      toggle?.addEventListener('click', openSidebar)

      overlay?.addEventListener('click', closeSidebar)

      // Close sidebar after clicking menu
      navLinks.forEach((link) => {
        link.addEventListener('click', closeSidebar)
      })
    </script>
  </body>
</html>
